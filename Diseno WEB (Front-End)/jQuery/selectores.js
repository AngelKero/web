$(document).ready(function () {
    //---Etiquetas
    //$('h1').hide();
    //$('h1, h2').hide();//Seleccionar varias

    //--Clases
    $('.encabezado').hide();

    //id's
    //$('#primero').hide();

    //$('p.primero').hide();//Ocultar todos los parrafos con la clase primero

    $('.primero a').hide();
});